# Tests

Run with:

```sh
python -m unittest discover -s tests -v
```

The suite covers schema meta-validation, descriptors/workspaces/archives, archive attacks, deterministic packing, migration provenance, RFC 8785/runtime generators, JSON-LD parsing, SHACL, OKF structure, generated files, metadata, and release-state invariants.
